# bash Arrays
arr=()
arr+=('foo')
arr+=('bar')

echo $arr # displays the first element only.
echo ${arr[0]} # arrays are zero-based, so displays 'foo'.
echo ${#arr[0]} # displays the length of the first element, so 3.
echo ${#arr[*]} # displays the length of the array, so 2.

